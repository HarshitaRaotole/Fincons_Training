spring.application.name=employee-api
server.port=8081
# 1. Database Connection
spring.datasource.url=jdbc:mysql://localhost:3306/employee_db
spring.datasource.username=root

spring.datasource.password=Harshita@2002

# 2. Hibernate Settings
# "update" will automatically create the tables based on your Java classes
spring.jpa.hibernate.ddl-auto=create
# Show the SQL queries in the console (good for learning)
spring.jpa.show-sql=true

# 3. Dialect (Optimizes SQL for MySQL)
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
