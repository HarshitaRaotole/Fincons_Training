spring:
  datasource:
    url: jdbc:mysql://localhost:3306/training_db
    username: root
    password: Harshita@2002

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8081
